const companyModel = require("../models/companyModel");exports.createCompany = async (req, res) => {    try {        const { name } = req.body;        const code = Math.floor(1000 + Math.random() * 9000);        companyModel.verifyCompany(code, function (err, companyResult) {            if (err) {                return res.status(500).json(err);            } else {                if (companyResult.length > 0) {                    return res.status(400).json({ success: false, message: 'Company already exists' });                } else {                    const companyData = { name, code };                    companyModel.createCompany(companyData, function (err, result) {                        if (err) {                            return res.status(500).json(err);                        } else {                            return res.status(201).json({ success: true, message: 'Company created successfully', company: result });                        }                    });                }            }        });    } catch (error) {        console.error('Error creating company:', error);        res.status(500).json({ success: false, message: 'Internal server error' });    }};