pipeline {    agent any    stages {        stage('Checkout') {            steps {                git 'https://github.com/SLK349/api-sso.git'            }        }        stage('Install dependencies') {            steps {                sh 'npm install'            }        }        stage('Build') {            steps {                sh 'npm run build'            }        }        stage('Deploy') {            steps {                sh 'npm run deploy'            }        }    }}